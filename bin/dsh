#! /bin/bash

# Change to the current directory inside a container
docker run -it --rm --network=bridge --security-opt=no-new-privileges --cap-drop=ALL --user $(id -u):$(id -g) -w $PWD -v $PWD:$PWD $@
